import * as chai from "chai";
import * as td from "testdouble";

import { Jira } from "./client";

const assert = chai.assert;

describe("Jira", async () => {

    beforeEach(done => {
        done();
    });

    afterEach(done => {
        td.reset();
        done();
    });

    describe("constructor()", async () => {

        it("something", async () => {

        });

    });

    describe("findIssue()", async () => {

        it("something", async () => {

        });

    });

});